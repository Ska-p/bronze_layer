"""
Configuration package exposing environment-driven settings.

This module exists to provide a stable import path (`config.config`)
both inside the Azure Batch container and for local executions.
"""
